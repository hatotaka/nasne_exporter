---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nasne-exporter-deployment
  namespace: prometheus
  labels:
    app: nasne-exporter
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nasne-exporter
  template:
    metadata:
      labels:
        app: nasne-exporter
    spec:
      containers:
      - name: nasne-exporter
        image: quay.io/hatotaka/nasne-exporter:v0.0.7-arm
        args:
          - --nasne-addr=10.0.1.34
          - --nasne-addr=10.0.1.30
          - --nasne-addr=10.0.1.31
          - --v=2
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
---
kind: Service
apiVersion: v1
metadata:
  name: nasne-exporter
  namespace: prometheus
spec:
  selector:
    app: nasne-exporter
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  type: NodePort
